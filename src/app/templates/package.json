{
  "name": "<%= name %>",
  "description": "<%= description %>",
  "version": "0.0.0",
  "author": {
    "name": "<%= user.name %>",
    "email": "<%= user.email %>"
  },
  "license": "MIT",
  "main": "src/index.js",
  "scripts": {
    "build": "gulp build",
    "dev": "gulp",
    "test": "standard && nyc mocha --require babel-register",
    "lint": "standard",
    "lint:fix": "standard --fix"
  },
  "standard": {
    "globals": [
      "after",
      "afterEach",
      "before",
      "beforeEach",
      "describe",
      "it"
    ]
  }
}
